# Arm-BigQuery-ML-on-Google-Cloud
GKE analytics worker optimized for Arm64 (Tau T2A). Leverages native Python/NumPy/Pandas Arm kernels for high-performance, cost-efficient stream processing using Kubernetes nodeSelector for mandatory scheduling.
